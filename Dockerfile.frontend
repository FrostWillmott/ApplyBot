FROM nginx:alpine

# Copy static files
COPY app/static /usr/share/nginx/html

# Copy nginx config directly (no template needed)
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
